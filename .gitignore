__pycache__/
*.pyc
.venv/

# Simulation / evaluation artifacts (regenerate with compare_algorithms.py or src/main.py)
outputs/**/*.csv
# PNGs under outputs/ are tracked so figures are visible on GitHub (CSV stays ignored).
